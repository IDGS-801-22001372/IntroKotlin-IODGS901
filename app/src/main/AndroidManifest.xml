<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntroKotlinIODGS901"
        tools:targetApi="31">
        <activity
            android:name=".operaciones.OperacionesActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Menú Principal (con botones Cinepolis y Diccionario) -->
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Diccionario -->
        <activity
            android:name=".diccionario.DicActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity
            android:name=".diccionario.BuscarActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity
            android:name=".diccionario.CapturaActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight" /> <!-- Cinepolis -->
        <activity
            android:name=".cinepolis.CinepolisActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight" /> <!-- Otros ejemplos -->
        <activity
            android:name=".SaludoActivity"
            android:exported="true" />
        <activity
            android:name=".ResultadoActivity"
            android:exported="true" />
        <activity
            android:name=".ejemplo2.SaludoActivity"
            android:exported="true" />
    </application>

</manifest>